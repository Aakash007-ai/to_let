import 'package:flutter/material.dart';

class OwnerSide extends StatelessWidget {
  const OwnerSide({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
